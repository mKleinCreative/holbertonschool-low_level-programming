#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: with proper grammar, but the outcome is a piece of art, (Success)
 */

int main(void)
{
	printf("%s\n", "with proper grammar, but the outcome is a piece of art,");

	return (0);
}
