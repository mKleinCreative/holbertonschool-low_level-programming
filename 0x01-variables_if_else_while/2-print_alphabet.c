#include <stdio.h>

/**
 * main - Entry point
 * prints out the alphabet in lowercase!
 * Return: (Success)
 */

int main(void)
{
	char ch;

	for (ch = 'a'; ch <= 'z' ; ch++)
		putchar(ch);
	
	return (0);
}

