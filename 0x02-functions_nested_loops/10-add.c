/**
 * add - Entry point
 * @a: one of two integers to add up to find the sum.
 * @b: two of two integers to add up to find the sum.
 *
 * Return: The sum given the two arguements. (Success)
 */

int add(int a, int b)
{
	int sum;

	sum = a + b;

	return (sum);
}

