/**
 * mul - Write a function that multiplies two ints
 * @a: first int to multiply by
 * @b: 2nd int to multiply by
 * Return: the product of a * b(Success)
 */

int mul(int a, int b)
{
	return (a * b);
}

