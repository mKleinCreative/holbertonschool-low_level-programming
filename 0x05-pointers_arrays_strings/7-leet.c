/**
 * leet - translates a string to lee7 speak
 * @ch: string to translate
 * Return: (Success)
 */

char *leet(char *ch)
{
	char swap[5][2];
       
	swap = {{a, A},{e, E},{o, O},{t, T},{l, L}};
	while (*ch++)
	{
	
	}
	return (0);
}

